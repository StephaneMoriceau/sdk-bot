box:
  id: openjdk
  ports:
    - $PORT

#Build our application 
build:
  steps:
    - script:
        cmd: ls -al
        cmd: cd bots-client-sdk-js-18.2.3.0
        cmd: ls -al
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: sdkbot
    - script:
        cmd: ls -al
