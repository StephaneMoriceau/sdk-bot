box:
  id: openjdk
  ports:
    - $PORT

#Build our application 
build:
  steps:
    - script:
        code: |
          ls -al
          cd bots-client-sdk-js-18.2.3.0
          ls -al
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: sdkbot
    - script:
        code: ls -al
