box:
  id: openjdk
  ports:
    - $PORT

#Build our application 
build:
  steps:
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: sdkbot
    - script:
        cmd: ls -al
