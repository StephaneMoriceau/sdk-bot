box:
  id: openjdk
  ports:
    - $PORT

#Build our application 
build:
  steps:
    - script:
        code: ls -al
        code: cd bots-client-sdk-js-18.2.3.0
        code: ls -al
    - internal/docker-build:
        dockerfile: Dockerfile
        image-name: sdkbot
    - script:
        code: ls -al
